import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'my-first-ionic-vue3-with-tabs-app',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
